server:
  port: 8080

spring:
  application:
    name: ShowHub

  datasource:
    url: jdbc:postgresql://localhost:5432/hub_api
    username: ltt4456
    password: password

  jpa:
    show-sql: true

  web:
    error:
      include-message: always

  jwt:
    access-token-expiration: 1000000000
